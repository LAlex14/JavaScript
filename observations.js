/**
 *
 * ? function arguments is an object (except arrow fct)
 *
 * ? typeof Object === function
 *
 * ? console.log(typeof obj) => object
 *
 * ? Primitive type is data that is not an object and has no methods:
 * ? boolean, null, undefined, string, symbol, number
 *
 * ? in browser, global: this === window
 *
 * ? function declaration is hoisted before var declaration
 *
 * ? undefined * undefined = NaN
 *
 * ? let and const cannot be accessed before it was declared
 *
 * ? IIFE syntax: (function () {...}()) && (function () {...})()
 *
 * ? function declaration:          function foo(){}
 * ? named function expression:     var foo = function bar(){}
 * ? anonymous function expression: var foo = function (){}
 *
 * ? When we pass an object to the function, we creates a local
 * ? variable that refers to that same obj.
 *
 * ? Math.floor(4.95) == 4
 * ?
 *
 * ? tot ce e declarat cu new, e object
 *
 * ? parseInt(string); parseFloat(string); Number(string);
 *
 * ? false == Nan, null, '', undefined, false, 0
 *
 * ? data types recognised by js: boolean, null, string, number
 *
 * ? return; => returns undefined






*/